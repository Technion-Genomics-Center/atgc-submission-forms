# Build output — regenerated by build.py, never committed.
dist/

# Timestamped copies made before any data write.
_backups/
data/_backups/

__pycache__/
*.py[cod]
.venv/
venv/
logs/
*.log

# Windows / Office cruft
Thumbs.db
desktop.ini
~$*

.claude/settings.local.json
.vscode/
.idea/

# The generated shotgun workbook is a handover artefact, not source.
Shotgun-metagenomics-*.xlsx

# Internal helper files for the lab (website go-live notes). Kept on the share,
# never committed: this repo is public, and these are not for researchers.
website/
